# MotionBrain Roadmap

MotionBrain의 로드맵은 "모터를 움직이는 프로젝트"가 아니라 "설명 가능한 로봇 제어 스택을 단계적으로 만드는 포트폴리오 프로젝트"라는 전제를 기준으로 정리한다.

## 최종 목표

최종 결과물은 다음을 모두 보여주는 로봇 개발 포트폴리오다.

- ESP32 기반 실시간 모터 제어
- 안전 상태 머신과 인터록
- STM32 기반 센서 허브
- ESP32-CAM 기반 영상 입력
- 센서 피드백 기반 판단 계층
- Raspberry Pi + ROS2 + AI 상위 제어
- 각 단계별 설계 이유와 한계를 설명 가능한 문서화

## 현재 위치

현재 프로젝트는 다음 상태까지 도달했다.

- ESP32 5축 오픈루프 모션 제어 완료
- 웹 UI, 시리얼 명령, 관절 추상화, 시퀀스 큐 구현 완료
- 실물 5개 모터 동작 테스트 완료
- STM32 `MPU-6050` 단독 검증 완료

즉, 현재 주 작업 단계는 `Phase 3`이다.

## 로드맵 원칙

- 안전이 항상 기능보다 우선이다.
- 현재 코드와 실물 검증 사실이 문서보다 우선이다.
- 포트폴리오 관점에서 "왜 이렇게 설계했는지" 설명 가능해야 한다.
- 확정 사실과 예정 계획을 혼동하지 않는다.
- `ESP32 only`에서 끝내지 않고 `RPi/ROS2`까지 연결한다.
- `ESP32-CAM + vision/AI`를 최종 데모 시나리오에 포함한다.

## 단계 구성

### Phase 1. 모션 제어 커널

목표:

- 상태 머신
- 안전 규칙
- 기본 모터 제어
- 시리얼 기반 제어

현재 상태:

- 완료

포트폴리오 포인트:

- 임베디드 안전 상태 설계
- 하드웨어 직결 제어의 기본기

### Phase 2. 5축 제어 인터페이스와 모션 추상화

목표:

- 웹 UI
- 관절 추상화
- 시퀀스 큐
- 서치라이트 같은 주변장치 제어

현재 상태:

- 사실상 완료

현재 증거:

- `RobotArm`
- `MotionSequence`
- `/joint`, `/sequence`, `/light` API
- 5모터 실물 테스트 완료

포트폴리오 포인트:

- 입력 채널 다양화
- 저수준 PWM을 의미 있는 관절 동작으로 추상화

### Phase 3. 센서 피드백과 판단 계층

목표:

- STM32 센서 허브 구축
- ESP32 센서 브리지 연결
- 입력/판단/모션 계층 분리
- 센서 기반 safety 동작
- 베이스 상대각 폐루프 제어

현재 상태:

- 진행 중

세부 단계:

1. `3-A Sensor Feedback Layer`
   - STM32에서 `MPU-6050`, `HC-SR04` 처리
   - ESP32에 UART 센서 패킷 송신
   - `distance -> stop`, `vibration -> FAULT`
2. `3-B Decision Layer`
   - `CommandBus`
   - `SafetyGate`
   - `Dispatcher`
3. `3-C Closed-Loop Motion`
   - 베이스 상대각 제어
   - 시퀀스와 센서 인터록 결합
4. `3-D Message Bridge`
   - 향후 RPi/ROS2 연결을 위한 메시지 인터페이스 정리

포트폴리오 포인트:

- 멀티 MCU 아키텍처
- 센서 통합과 실시간 안전 처리
- 계층 분리 설계

### Phase 4. Raspberry Pi + ROS2 + Vision/AI 상위 제어

목표:

- ESP32를 실시간 모션 제어 노드로 유지
- ESP32-CAM을 영상 입력 노드로 추가
- Raspberry Pi에서 상위 판단, 비전 처리, ROS2 통합
- 센서/상태/명령 메시지 구조 정리
- `detect -> align -> pick` 데모 시나리오를 구성

예상 결과:

```text
[STM32 Sensor Hub] -> [ESP32 Motion Controller] <-> [Raspberry Pi + ROS2 + AI]
                         ^
                         |
                   [ESP32-CAM]
```

포트폴리오 포인트:

- ROS2 메시지 설계
- 분산 제어 구조
- 실시간 노드와 고수준 노드의 역할 분리
- 카메라 입력과 로봇 동작 연결
- 비전 결과를 베이스 정렬과 집기 시퀀스로 연결

### Phase 5. 포트폴리오 패키징

목표:

- 아키텍처 다이어그램
- 하드웨어/소프트웨어 구성 정리
- 데모 시나리오 정의
- 면접에서 설명 가능한 결과물 패키지화

산출물:

- 최종 README
- 시스템 구조도
- 단계별 데모 영상/로그
- 설계 선택 이유와 한계 정리

## 현재 기준 우선순위

1. `Phase 3-A` 센서 허브 MVP 완료
2. `Phase 3-B` 판단 계층 분리
3. `Phase 3-C` 베이스 상대각 제어
4. `Phase 3-D` 메시지 인터페이스 정리
5. `Phase 4` Raspberry Pi + ROS2 연결
6. `Phase 5` 포트폴리오 패키징

## 현재 완료 / 남은 것

### 완료

- ESP32 모션 제어 커널
- 웹 UI
- 시리얼 제어
- 5축 관절 추상화
- 시퀀스 큐
- 5모터 실물 동작 테스트
- STM32 `MPU-6050` 통신 검증

### 남음

- STM32 `HC-SR04`
- STM32 -> ESP32 센서 스트림
- ESP32 safety 연동
- 판단 계층 분리
- 폐루프 제어
- ESP32-CAM 스트리밍
- 비전 기반 정렬 로직
- Raspberry Pi + ROS2 + AI 연결
- 포트폴리오 마감 문서화

## 현재 핵심 리스크

- 문서가 실제 진행 상태를 못 따라가면 다음 단계 판단이 계속 흔들린다.
- `MPU-6050`만으로는 장기 절대 yaw 기준이 불안정하다.
- ROS2를 너무 일찍 붙이면 센서/안전 계층이 불안정한 상태로 상위 구조만 커질 수 있다.
- ESP32-CAM 단독 AI 추론보다 상위 노드에서 비전/AI를 처리하는 구조가 현실적일 가능성이 높다.

## 문서 운영 방침

- `README.md`: 현재 상태와 프로젝트 소개
- `로드맵.md`: 장기 목표와 단계 구조
- `PHASE3_PLAN.md`: 지금 당장 구현할 실행 계획
- `PIN_MAP.md`: 하드웨어 핀 참조
- `PHASE1-5_STEPS.md`: archive 성격의 bring-up 기록

## 한 줄 요약

MotionBrain은 ESP32 5축 모션 제어 프로젝트에서 시작했지만, 최종적으로는 STM32 센서 허브, ESP32-CAM 비전 입력, Raspberry Pi + ROS2 + AI까지 연결되는 로봇 제어 포트폴리오로 완성하는 것이 목표다.
