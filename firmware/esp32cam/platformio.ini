; MotionBrain ESP32-CAM vision node
;
; Upload separately from the main ESP32 motion controller:
;   pio run -d firmware/esp32cam
;   pio run -d firmware/esp32cam -t upload

[env:esp32cam]
platform = espressif32
board = esp32cam
framework = arduino
monitor_speed = 115200
upload_speed = 115200
